# 🛠️ 开发者工具说明 (Developer Tools)
为了方便项目的素材扩展和硬件适配，我们在 tools/ 目录下提供了两款核心工具：
## 1. 素材全自动切割提取器 (tool_pure_manual.py)
这是本项目最核心的素材生产工具。它允许你直接从游戏录屏视频中，通过可视化点击和框选，快速提取出规范的技能图标和角色头像。
- 功能特点： 
  - 所见即所得：支持视频进度条拖动与空格暂停，精准锁定图标最亮瞬间。 
  - 双模式切换：按键盘 1 进入角色头像模式（框选即自动保存），按 2 进入技能图标模式（框选后提示手动命名）。 
  - 自动处理：无需手动测量坐标，程序会自动计算选区并生成 .png 文件。 
- 使用方法： 
  1. 修改脚本中的 VIDEO_PATH 为你的游戏录像路径。 
  2. 运行脚本，在想要截取的画面按 空格 暂停。 
  3. 用鼠标左键框选图标区域，松开即可完成保存。
## 2. 手柄硬件深度检测工具 (deep_check_input.py)
由于不同品牌手柄（Xbox, PS, 第三方）在 Windows 下的按键 ID（Button ID）和轴号（Axis ID）完全不同，该工具用于获取你手中设备的真实硬件参数。
- 功能特点： 
  - 实时报告：实时打印按下按键的 ID。 
  - 轴体识别：自动识别 LT/RT 等线性扳机键的轴号及数值变化。 
  - Hat 监测：报告 D-Pad（十字键）的坐标值。
- 使用方法：
  1. 连接手柄并运行脚本。
  2. 依次按下手柄所有按键，记录控制台输出的数字。 
  3. 根据记录的数字，更新项目根目录 config.json 中的 controller -> hardware_mapping 部分，以完成硬件适配。
## 💡 提示：如何贡献新角色？
  如果你想为项目增加新角色支持：
  1. 使用 tool_pure_manual.py 提取该角色的： 
     - character/avatar.png (变奏图标)
     - resonance_skill/normal_skill.png (技能图标)
     - resonance_liberation/liberation.png (大招图标)
     - 以及对应的强化状态图标。
  2. 在 Character_Occupation.txt 中添加角色的武器映射映射（例如 Jinhsi=broadblade）。
  3. 在 configs/ 下编写对应的 .py 连招剧本。